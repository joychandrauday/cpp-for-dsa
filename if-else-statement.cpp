#include <iostream>
using namespace std;
int main()
{

    int a;
    cout << "Enter any integar ";
    cin >> a;
    if (a > 0)
    {
        cout << a << " is a positive number.";
        cout << a << " is a positive number.";
    }
    else if (a < 0)
    {
        cout << a << " is a negative number.";
    }
    return 0;
}